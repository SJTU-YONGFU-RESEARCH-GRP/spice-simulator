{
  "name": "spice-simulator",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "homepage": "https://sjtu-yongfu-research-grp.github.io/spice-simulator/",
  "description": "Browser SPICE-like circuit simulator (DC + TRAN)",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "serve": "node scripts/serve.mjs",
    "test": "node scripts/smoke.mjs",
    "build:release": "node scripts/build-release.mjs",
    "build:release:copy": "node scripts/build-release.mjs --skip-obfuscate",
    "release": "bash scripts/release.sh",
    "release:dry": "bash scripts/release.sh --dry-run",
    "release:wsl": "wsl -e bash -lc \"cd /mnt/d/proj/spice-simulator && ./scripts/release.sh\""
  },
  "devDependencies": {
    "terser": "^5.39.0"
  }
}
